# Install and setup the following components for each system in the following order.
---
macosx:
  - dotfiles
  - homebrew # This need's to run first due to cmd dependency
  - localrc
  - shell
  - git
  - vim
  - macosx
  - node
  - ruby
  - zsh # Install zsh last and then swtich!

linux:
